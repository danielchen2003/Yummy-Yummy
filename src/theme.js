import { createTheme } from "@mui/material"
export const theme = createTheme({
  palett: {
    primary: {
      main: "#f04328",
      light: "#f04328",
      dark: "#f04328",
    },
    secondary: {
      main: "#8acedb",
    },
    otherColor: {
      main: "#999",
    },
  },
})
